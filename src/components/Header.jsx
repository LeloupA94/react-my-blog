import Title from "./Title";
import Baseline from "./Baseline";
const Header = () => {
  console.log(Title);
  return (
    <header>
      <Title />
      <Baseline />
    </header>
  );
};
export default Header;
